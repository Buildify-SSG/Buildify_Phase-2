<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wareflow.buildify.mapper.OutboundMapper">

    <select id="outboundlist" resultType="com.wareflow.buildify.vo.OutboundVO">
        select o.client_id as clientId,
               p.prod_name as prodName,
               o.quantity as quantitiy
               o.req_outbound_date as reqOutboundDate,
               o.outbound_process_date as outboundProcessDate,
               o.ware_id as wareId,
               o.status as status
        form outbound o join product p ON o.pred_id = p.prod_id

    </select>
    <!-- 여기에 SQL 정의 들어감 -->
</mapper>