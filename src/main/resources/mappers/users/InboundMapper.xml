<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wareflow.buildify.domain.user.inbound.mapper.UserInboundMapper">
    <!-- 여기에 SQL 정의 들어감 -->

    <select id="inboundList" resultType="com.wareflow.buildify.vo.ProductVO">
        select             prod_id AS prodId,
                           brand,
                           prod_name AS prodName,
                           prod_price AS prodPrice,
                           prod_code AS prodCode,
                           prod_size AS prodSize,
                           prod_categoryid AS prodCategoryid,
                           client_id AS clientId

        from product where client_id = #{clientId}
    </select>

    <select id="inboundInsertlist" resultType="com.wareflow.buildify.vo.InboundProductVO">
        select inbound.inbound_id as inboundId,
               inbound.prod_id as prodId,
               inbound.client_id as clientId,
               inbound.quantity as quantity,
               inbound.inbound_status as inboundStatus,
               inbound.req_inbound_date as reqInboundDate,
               inbound.ware_id as wareId,
               inbound.warehouse_pos_x as warehousePosX,
               inbound.warehouse_pos_y as warehousePosY,
               inbound.inbound_process_date as inboundProcessDate,
               product.prod_name as prodName,
               product.prod_price as prodPrice
        from inbound JOIN product ON inbound.prod_id = product.prod_id  where inbound.client_id = #{clientId}
    </select>

    <select id="searchInboundList" resultType="com.wareflow.buildify.dto.InboundProduntDTO">
        SELECT i.*, p.prod_name
        FROM inbound i
        JOIN product p ON i.prod_id = p.prod_id
        WHERE
        <choose>
            <when test="searchType == 'prodId'">
                i.prod_id LIKE #{keyword}
            </when>
            <when test="searchType == 'brand'">
                p.brand LIKE #{keyword}
            </when>
            <when test="searchType == 'prodName'">
                p.prod_name LIKE #{keyword}
            </when>
            <when test="searchType == 'wareId'">
                i.ware_id LIKE #{keyword}
            </when>
            <otherwise>
                1 = 1
            </otherwise>
        </choose>
    </select>

    <select id="searchInboundInsertList" resultType="com.wareflow.buildify.dto.ProductDTO">
        SELECT i.*, p.prod_name
        FROM inbound i
        JOIN product p ON i.prod_id = p.prod_id
        WHERE
        <choose>
            <when test="searchType == 'prodId'">
                i.prod_id LIKE #{keyword}
            </when>
            <when test="searchType == 'brand'">
                p.brand LIKE #{keyword}
            </when>
            <when test="searchType == 'prodName'">
                p.prod_name LIKE #{keyword}
            </when>
            <when test="searchType == 'wareId'">
                i.ware_id LIKE #{keyword}
            </when>
            <otherwise>
                1 = 1
            </otherwise>
        </choose>
    </select>
<!--selectΩ-->

    <!-- user_id를 통해 client_id 조회 -->
    <select id="findClientIdByUserId" resultType="string">
        SELECT client_id
        FROM user
        WHERE user_id = #{userId}
    </select>

    <select id="getInboundInsert" parameterType="list" resultType="com.wareflow.buildify.dto.ProductDTO">
        SELECT prod_id, prod_name, prod_price, prod_size
        FROM product
        WHERE prod_id IN
        <foreach item="id" collection="prodId" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <insert id="insertInbound" parameterType="com.wareflow.buildify.vo.InboundVO">
        INSERT INTO inbound (
            inbound_id,
            prod_id,
            client_id,
            quantity,
            ware_id,
            inbound_status,
            req_inbound_date
        )
        VALUES (
                   #{inboundId},
                   #{prodId},
                   #{clientId},
                   #{quantity},
                   #{wareId},
                   #{inboundStatus},
                   #{reqInboundDate}
               )
    </insert>

    <select id="insertware" parameterType="com.wareflow.buildify.vo.ProductVO" resultType="com.wareflow.buildify.vo.UserWareHouseVO">

        select ware_id, warehouse_pos_x, warehouse_pos_y, warehouse_usage, contract_area
        from userWareHouse
        where client_id = #{clientId}

    </select>

    <select id="getProdSizeAsString" resultType="java.math.BigDecimal">
        SELECT prod_size
        FROM product
        WHERE prod_id = #{prodId}
    </select>

</mapper>