<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mybatis="http://mybatis.org/schema/mybatis-spring" xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd http://mybatis.org/schema/mybatis-spring http://mybatis.org/schema/mybatis-spring.xsd http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd">

    <context:component-scan base-package="com.wareflow.buildify">
    </context:component-scan>
  
    <mybatis:scan base-package="
    com.wareflow.buildify.domain.auth.signup.mapper,
    com.wareflow.buildify.domain.auth.login.mapper,
    com.wareflow.buildify.domain.admin.inbound.mapper,
    com.wareflow.buildify.domain.admin.inventory.mapper,
    com.wareflow.buildify.domain.admin.outbound.mapper,
    com.wareflow.buildify.domain.admin.product.mapper,
    com.wareflow.buildify.domain.admin.systemOperation.mapper,
    com.wareflow.buildify.domain.admin.userManagement.mapper,
    com.wareflow.buildify.domain.user.product.mapper,
    com.wareflow.buildify.domain.user.outbound.mapper,
    com.wareflow.buildify.domain.user.inbound.mapper,
    com.wareflow.buildify.domain.user.inventory.mapper
    "/>

    <!-- properties 불러오기 -->
    <context:property-placeholder location="classpath:application-secret.properties"/>

    <!-- hikariConfig에서 하드코딩 대신 프로퍼티 사용 -->
    <bean id="hikariConfig" class="com.zaxxer.hikari.HikariConfig">
        <property name="driverClassName" value="${application-secret.driver}" />
        <property name="jdbcUrl" value="${application-secret.url}" />
        <property name="username" value="${application-secret.username}" />
        <property name="password" value="${application-secret.password}" />
        <property name="dataSourceProperties">
            <props>
                <prop key="cachePrepStmts">true</prop>
                <prop key="prepStmtCacheSize">250</prop>
                <prop key="prepStmtCacheSqlLimit">2048</prop>
            </props>
        </property>
    </bean>

    <bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource" destroy-method="close">
        <constructor-arg ref="hikariConfig"></constructor-arg>
    </bean>

    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="mapperLocations" value="classpath:/mappers/**/*.xml"></property>
    </bean>

    <tx:annotation-driven transaction-manager="transactionManager" />

    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>

</beans>